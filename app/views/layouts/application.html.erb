<!DOCTYPE html>
<html>
<head>
  <meta http-equiv="content-type" content="text/html;charset=UTF-8" />
  <meta charset="utf-8" />
  <title>Vanzeiro - Aproveite seu tempo!</title>
  <meta name="viewport" content="width=device-width, initial-scale=1.0, maximum-scale=1.0, user-scalable=no" />
  <meta content="" name="description" />
  <meta content="" name="author" />

  <%= favicon_link_tag 'bus.ico' %>
  <%= render 'layouts/head' %>

  <%= csrf_meta_tags %>
</head>
<% if current_page?(controller: 'devise/sessions', action: 'new') || current_page?(entrar_path) %>
    <body class="error-body no-top lazy"  data-original="<%= path_to_image('work.jpg') %>"
          style="background-image: url(<%= path_to_image('work.jpg') %>)">

    <%= yield %>

    <%= render 'layouts/footer' %>
    </body>
<% elsif current_user.blank? %>
    <body class="">
    <div class="main-wrapper">

      <% if current_page?(pricing_path)  || current_page?(contato_path) %>

      <% else %>
          <header id="ha-header" class="ha-header ha-header-hide "  >
            <div class="ha-header-perspective">
              <div class="ha-header-front navbar navbar-default">
                <div class="compressed">
                  <div class="navbar-header">
                    <button data-target=".navbar-collapse" data-toggle="collapse" class="navbar-toggle" type="button">
                      <span class="sr-only">Toggle navigation</span>
                      <span class="icon-bar"></span>
                      <span class="icon-bar"></span>
                      <span class="icon-bar"></span>
                    </button>
                    <a href="#" class="navbar-brand compressed"><img src="/assets/logo-b.png" alt="" data-src="/assets/logo-b.png" data-src-retina="/assets/logo2x.png" width="119" height="22"/></a>
                  </div>
                  <div class="navbar-collapse collapse">
                    <ul class="nav navbar-nav navbar-right" >
                      <li><%= link_to 'Home', root_path %></li>
                      <li><%= link_to 'Entrar', entrar_path %></li>
                      <li><%= link_to 'Planos', pricing_path %></li>
                      <li><%= link_to 'Contato', contato_path %></li>
                    </ul>
                  </div><!--/.nav-collapse -->
                </div>

              </div>
            </div>
          </header>
      <% end %>

      <%= render 'layouts/header' %>
      <%= yield %>
    </div>

    <%= render 'layouts/footer' %>
    </body>
<% else %>
    <body class="<%= yield(:body_class) %>">

    <%= render 'layouts/header' %>
    <%= yield %>
    <%= render 'layouts/footer' %>

    </body>
<% end %>
</html>
