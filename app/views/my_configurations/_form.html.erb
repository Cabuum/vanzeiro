<%= form_for(@my_configuration) do |f| %>
  <% if @my_configuration.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@my_configuration.errors.count, "error") %> prohibited this my_configuration from being saved:</h2>

      <ul>
      <% @my_configuration.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :business_day_for_payments %><br>
    <%= f.number_field :business_day_for_payments %>
  </div>
  <div class="field">
    <%= f.label :user_id %><br>
    <%= f.text_field :user_id %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
